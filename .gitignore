# Rust
/target
**/*.rs.bk
Cargo.lock.orig

# Fixture-extraction Python tooling
.venv/
__pycache__/
*.pyc
.python-version
.pytest_cache/
.ruff_cache/
.mypy_cache/

# OS / editor
.DS_Store
*.swp
.idea/
.vscode/

# Scratch
/scratch
*.log

# Benchmark run outputs (regenerated by `just bench`; the committed artifacts are
# dev-notes/benchmarks.md + dev-notes/benchmarks.json).
/tools/bench/results/

# Private cargo target dir (AGENTS.md: CARGO_TARGET_DIR=target-private)
/target-private

# Shared cross-worktree cargo target dir (large; never committed)
/target-shared
